<!DOCTYPE html>
<html>
  <head>
    <title>PastePile</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <style>
      input {
          margin-bottom: 10px;
      }
    </style>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
      <div class="container-fluid">
        <a class="navbar-brand" href="/">PastePile</a>
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <a class="nav-link" href="/">New Paste</a>
          </li>
          <% if @current_user %>
            <li class="nav-item">
              <a class="nav-link" href="<%= user_path(@current_user) %>">My account</a>
            </li>
            <% if @current_user.admin %>
              <li class="nav-item">
                <a class="nav-link" href="<%= users_path %>">Accounts</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="/pastes">Pastes</a>
              </li>
            <% else %>
              <li class="nav-item">
                <a class="nav-link" href="/pastes">My Pastes</a>
              </li>
            <% end %>
          <% else %>
            <li class="nav-item">
              <a class="nav-link" href="<%= signin_path %>">Sign in</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="<%= signup_path %>">Sign up</a>
            </li>
          <% end %>
        </ul>
          <% if @current_user %>
            <span class="navbar-text">
              <small>Signed in as <strong><%= @current_user.username %>
                <% if @current_user.admin %>
                  (admin<%= ', <span style="color: #dc3545">superuser</span>'.html_safe if @current_user.superuser %>)
                <% end %>,
              </strong></small>
            </span>
            <span class="nav-item">
              <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <small><strong>
                <%= link_to 'Sign out', {controller: :sessions, action: :destroy, id: session[:session_id]}, method: :delete, class: 'nav-link'%>
                </strong></small>
              </ul>
            </span>
          <% end %>
        </>
      </div>
    </nav>
    <%= yield %>
  </body>
</html>